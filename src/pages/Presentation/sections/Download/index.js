import React, { forwardRef } from "react";

const Download = forwardRef((props, ref) => {
  return (
    <div ref={ref}>
      {/* Existing Download component code */}
      console.log("Rendering Download section");
      ...existing code...
    </div>
  );
});

export default Download;